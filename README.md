# Crypto Streaming

Streaming crypto prices from Binance into Kafka with Python, then consuming them for downstream storage (e.g., Snowflake).

This project is the foundation for a Kafka + Kubernetes + AWS + Snowflake pipeline.
